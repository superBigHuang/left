spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/left
  jpa:
    hibernate:
      ddl-auto: update
#      enable_lazy_load_no_trans: true
    show-sql: true
    open-in-view: true
logging:
  config: log4j2-spring.xml

