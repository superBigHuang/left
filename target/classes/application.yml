spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/left
  jpa:
    hibernate:
      ddl-auto: update
#      enable_lazy_load_no_trans: true
    show-sql: true
    open-in-view: true
  redis:
    host: localhost
    database: 0
    port: 6379
    jedis:
      pool:
        max-idle: 300
        max-wait: 3000
